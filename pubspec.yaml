name: portfolio_app
description: "Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  http: ^1.6.0
  flutter_bloc: ^8.1.4
  equatable: ^2.0.8
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^9.1.7
  mockito: ^5.6.4
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
